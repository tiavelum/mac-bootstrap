# Brewfile — apps installable via Homebrew
# Usage: brew bundle --file=Brewfile

cask "google-chrome"
cask "whatsapp"
cask "onedrive"
cask "visual-studio-code"
cask "claude"          # Claude desktop app
cask "logi-options+"   # Logitech MX Keys: remap the screenshot key (needs a reboot)

# Microsoft Office (Word, Excel, ...) — uncomment if not installed yet
# cask "microsoft-office"

# Clipboard manager with history — not installed yet, see setup-docs/setup-04
# cask "maccy"
